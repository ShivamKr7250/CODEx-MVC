@using CODEx.Model.View_Models

@model FacultyVM

<div class="card shadow border-0 mt-4">
    <div class="card-header bg-secondary bg-gradient ml-0 py-3">
        <div class="row text-center">
            <h2 class="text-primary py-2"> @(Model.Faculty.Id!=0?"Update":"Create") Coordinator</h2>
        </div>
    </div>
    <div class="card-body p-4">
        <form method="post" class="row" enctype="multipart/form-data">
            <input asp-for="Faculty.Id" hidden />
            <input asp-for="Faculty.ImageUrl" hidden />
            <div class="row">
                <div class="col-10">
                    <div class="border p-3">
                        @*<div asp-validation-summary="ModelOnly"></div>*@
                        <div class="mb-3 row">
                            <label asp-for="Faculty.Name" class="p-1"></label>
                            <input asp-for="Faculty.Name" class="form-control border-0 shadow" />
                            <span asp-validation-for="Faculty.Name" class="text-danger"></span>
                        </div>
                        <div class="mb-3 row">
                            <label asp-for="Faculty.Desigation" class="p-1"></label>
                            <input asp-for="Faculty.Desigation" class="form-control border-0 shadow" />
                            <span asp-validation-for="Faculty.Desigation" class="text-danger"></span>
                        </div>
                        <div class="mb-3 row">
                            <label asp-for="Faculty.ImageUrl" class="p-1 text-muted"></label>
                            <input type="file" name="file" asp-for="Faculty.ImageUrl" class="form-control border-0 shadow" />
                        </div>
                        <div class="row">
                            <div class="col-6 col-md-3">
                                @if (Model.Faculty.Id != 0)
                                {
                                    <button type="submit" class="btn btn-primary form-control">Update</button>
                                }
                                else
                                {
                                    <button type="submit" class="btn btn-primary form-control">Create</button>
                                }
                            </div>
                            <div class="col-6 col-md-3">
                                <a asp-controller="Faculty" asp-action="Index" class="btn btn-outline-secondary border form-control">
                                    Back to List
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-2">
                    <img src="@Model.Faculty.ImageUrl" width="100%" style="border-radius:5px; border:1px solid #bbb9b9"
                </div>

            </div>

        </form>
    </div>
</div>



@section Scripts{
    @{
        <partial name="_ValidationScriptsPartial" />
    }
    
}